# HTTP — redirect to HTTPS in production, serve directly in dev
server {
    listen 80;
    server_name cartpath.app www.cartpath.app;

    # In production with SSL configured, uncomment the next line and remove everything below:
    # return 301 https://$server_name$request_uri;

    # Static files (React SPA)
    root /usr/share/nginx/html;
    index index.html;

    # API proxy
    location /api/ {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 10s;
    }

    # Data files proxy
    location /data/ {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_cache_valid 200 1h;
    }

    # SPA fallback — serve index.html for client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # Service worker — no cache
    location = /sw.js {
        expires off;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml image/svg+xml;
    gzip_min_length 1000;
}

# HTTPS server — enable when Let's Encrypt is configured
# 1. Run: certbot certonly --webroot -w /usr/share/nginx/html -d cartpath.app
# 2. Uncomment this block
# 3. Uncomment "return 301" in the HTTP server above
# server {
#     listen 443 ssl http2;
#     server_name cartpath.app www.cartpath.app;
#
#     ssl_certificate /etc/letsencrypt/live/cartpath.app/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cartpath.app/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location /api/ {
#         proxy_pass http://api:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 10s;
#     }
#
#     location /data/ {
#         proxy_pass http://api:8000;
#         proxy_set_header Host $host;
#         proxy_cache_valid 200 1h;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location = /sw.js {
#         expires off;
#         add_header Cache-Control "no-store, no-cache, must-revalidate";
#     }
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml image/svg+xml;
#     gzip_min_length 1000;
# }
